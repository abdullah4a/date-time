export interface DateTimeInput { 
    startDate: string, 
    endDate: string 
}
export interface DateTimePayload { 
    startDate: string, 
    endDate: string, 
    startTime: string, 
    endTime: string 
}